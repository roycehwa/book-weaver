# Migrated to pyproject.toml — use: uv sync --extra dev --extra workspace
# BookWeaver workspace API dependencies (kept for pip/CI reference)

# Web Framework — 0.115.6+ required for httpx>=0.28 TestClient compatibility
fastapi==0.115.6
uvicorn>=0.27.0
python-multipart>=0.0.6

# PDF Processing
pymupdf==1.23.8

# HTTP Client — docling requires >=0.28; pairs with fastapi>=0.115.6
httpx>=0.28
requests>=2.32.0

# Data Validation
pydantic>=2.11.4
pydantic-settings>=2.3.0

# Environment
python-dotenv>=1.0.0

# Testing
pytest>=8.3.5
pytest-asyncio>=0.23.0
